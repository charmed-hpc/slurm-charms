[project]
name = "slurmrestd"
version = "0.0"
requires-python = "==3.12.*"
dependencies = [
    "ops",
    "slurmutils",
    "charmed-hpc-libs[ops] >= 0.1.0",
    "charmed-slurm-slurmrestd-interface",
    "slurm-ops"
]

[tool.repository]
